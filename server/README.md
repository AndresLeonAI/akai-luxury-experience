# AKAI Server

Backend API + Stripe webhooks + Postgres (Prisma).

## Development

1. Install deps:
   - `npm install`
2. Copy env:
   - `cp .env.example .env` (or create `.env`)
3. Run migrations:
   - `npm run prisma:migrate`
4. Start server:
   - `npm run dev`

